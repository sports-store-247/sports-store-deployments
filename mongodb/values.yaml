image:
  registry: docker.io
  repository: bitnami/mongodb
  tag: latest

auth:
  enabled: true
  rootUser: root
  rootPassword: sports-store-local-password

persistence:
  enabled: false

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
