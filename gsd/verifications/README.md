# gsd/verifications

Stage 4 of the GSD loop. Evidence a change works — `kubectl`/`helm` output, checks,
and other proof captured after execution.

Naming: `<topic>-<slug>.md`. See [`../../GSD.md`](../../GSD.md).
